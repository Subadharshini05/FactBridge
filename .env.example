# Environment Variables for FactBridge

# Application
APP_NAME=FactBridge
ENV=development

# File Storage
UPLOAD_DIR=data/uploads

# AI / Vector DB (to be added later)
EMBEDDING_MODEL=placeholder
VECTOR_DB=local

# NOTE:
# Never commit real API keys.
# Use .env file locally (ignored by git).
